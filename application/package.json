{
  "name": "kong-campanion",
  "version": "1.0.0",
  "description": "This container contains docker-gen, certbot and node which will interact with Kong and all running containers in order to use and add a lets encrypt certificates with automatically creation and renewal",
  "repository": {
    "type": "git",
    "url": "https://github.com/slidewiki/kong-companion"
  },
  "main": "manageAPIs.js",
  "scripts": {
    "clean": "rm -R ./node_modules/ ./coverage/",
    "lint": "eslint -c .eslintrc ./**/*.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "engines": {
    "node": ">=8.10"
  },
  "author": "Kurt Junghanns",
  "license": "MPL-2.0",
  "dependencies": {
    "request": "^2.81.0"
  },
  "devDependencies": {
    "eslint": "^4.0.0",
    "eslint-plugin-promise": "^3.4.0"
  }
}
